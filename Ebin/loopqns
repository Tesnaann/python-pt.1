#factorial of numbers from 3 to 7
'''for k in range(3,8):
    f=1
    for g in range(1,k+1):
        f=f*g
    print("factorial",f)'''

#count vowels in a string
'''a=(input("Enter string"))
d=dict()
for k in a:
    if k in "aeiouAEIOU":
        if k in d:
            d[k]=d[k]+1
        else:
            d[k]=1
for k in d:
    print(f"{k}={d[k]}") # to print as a=1, b=2 rather {a:1}'''

#print add of numbers upto n with functions
def add(*args):
    s=0
    for k in args:
        s=s+k
    return s
print(add(2,10,19))


